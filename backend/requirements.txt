# DALSAN İSG — gerekli paketler
# Kontrol Paneli'ndeki "İlk Kurulumu Yap" düğmesi bu dosyayı kurar.
fastapi
uvicorn[standard]
jinja2
python-dotenv
python-multipart
pytest
httpx
ruff

# --- Görüntü analizi ---
# Tespit modeli ONNX Runtime ile çalışır (ADR-002: Apache-2.0 lisanslı YOLOX;
# torch'a gerek yok). supervision, ByteTrack sürümü değiştiği için sabitlendi.
# SÜRÜMLER SABİT: en yeni sürümler her platform için hazır paket (wheel)
# yayınlamıyor. opencv-python 5.x yalnız macOS 13+/arm64 ve macOS 14+/x86_64
# için tekerlek veriyor; Intel Mac'te pip kaynaktan derlemeye kalkıyor ve
# "İlk Kurulumu Yap" 10 dakika hata basıp başarısız oluyordu.
opencv-python==4.10.0.84
# ONNX Runtime (docs/17 S19, docs/16 §5): 1.30.0. İçine gömülü onnx 1.22.0
# model yükleme yolunu sertleştirir (CVE-2026-14647: onnx 1.21.x ve öncesi,
# 1.19.2 onnx 1.16.1 taşıyordu) ve aynı makinede CPU çıkarımı ~%25 hızlı.
# Intel Mac'te x86_64 tekerleği olan SON sürüm 1.23.2'dir (1.24.1 ve sonrası
# yalnız arm64); orada pip kaynaktan derlemeye kalkmasın diye 1.23.2 kurulur.
# 1.23.2'nin CVE durumu doğrulanmadı — Intel Mac yalnız geliştirme içindir.
onnxruntime==1.30.0; sys_platform != "darwin" or platform_machine != "x86_64"
onnxruntime==1.23.2; sys_platform == "darwin" and platform_machine == "x86_64"
numpy
supervision==0.25.1

# Windows'ta saat dilimi veritabanı işletim sistemiyle GELMEZ; bu paket
# olmadan Europe/Istanbul bulunamaz (kod yedeğe düşer ama doğrusu budur)
tzdata; sys_platform == "win32"
