.env
# Ayarlar sayfasının atomik yazımından kalabilecek geçici dosya
.env.yeni
veri/
# İzleme ekranının uygulama penceresi için ayrı tarayıcı profili
# (masaustu/uygulama_penceresi.py). veri/ DIŞINDADIR: yedeklemeye
# girmemeli — içinde kullanıcı verisi değil, önbellek vardır.
tarayici-profili/
models/*.pt
models/*.onnx
models/*.pth
.venv/
__pycache__/
*.pyc
.DS_Store
.pytest_cache/
.ruff_cache/
models/*.part
# indir.sh'in doğrulayamayıp kenara aldığı eski model dosyası
models/*.eski

# Claude Design tuvali (yerel tasarim calismasi, depoya girmez)
DALSAN İleri Fabrika Tasarımı/
*.dc.html

# PyInstaller / paketleme çıktısı (yüzlerce MB olur, depoya girmez)
build/
dist/
# PyInstaller depo kökünde kendiliğinden .spec üretir; o geçicidir.
*.spec
# Bizim yazdığımız üretim tarifi ise KAYNAK dosyadır, depoda durur.
!paketleme/*.spec
